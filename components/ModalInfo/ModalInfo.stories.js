import ModalInfo from './ModalInfo';

const Component = {
  title: 'ModalInfo',
  component: ModalInfo,
};

const Template = () => <ModalInfo />;

export const Default = Template.bind({});
export default Component;
